server:
  port: 8082

spring:
  application:
    name: springboot-mysql-demo
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
    default-property-inclusion: non_null
  profiles:
    active:
      - druid

